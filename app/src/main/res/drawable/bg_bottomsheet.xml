<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Option 1: Use a color defined in your colors.xml -->
    <!-- Make sure 'white' is defined in your res/values/colors.xml -->
    <solid android:color="@color/white" />

    <!-- Option 2: Use a theme attribute for better theme adaptability -->
    <!-- <solid android:color="?attr/colorSurface" /> -->
    <!-- If using Material Components, colorSurface is a good choice for surfaces like bottom sheets -->

    <!-- Option 3: Use Android's default background color (can vary with theme) -->
    <!-- <solid android:color="?android:attr/colorBackground" /> -->

    <corners
        android:topLeftRadius="16dp"
        android:topRightRadius="16dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>
